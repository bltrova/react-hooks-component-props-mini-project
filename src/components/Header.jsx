function Header({name}) {
    return (
        <header>
            <h1>{name}</h1>
        </header>
    )
}
//  create a header tag with h1 and the name of the Blog inside, passed as a prop called name

export default Header;